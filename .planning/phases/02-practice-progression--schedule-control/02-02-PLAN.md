---
phase: 02-practice-progression--schedule-control
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - app/(tabs)/practice.tsx
  - app/(tabs)/settings.tsx
  - app/(tabs)/index.tsx
  - lib/practices.ts
  - lib/storage.ts
autonomous: true
must_haves:
  truths:
    - "User can see a recommended practice based on their selected track"
    - "User can select a practice outside the schedule"
    - "User can opt into an extended 8-week track"
  artifacts:
    - path: "app/(tabs)/practice.tsx"
      provides: "Practice selection + recommended practice UI"
    - path: "app/(tabs)/settings.tsx"
      provides: "Track selection controls"
  key_links:
    - from: "app/(tabs)/practice.tsx"
      to: "lib/practices.ts"
      via: "getTodayPractice"
      pattern: "getTodayPractice"
---

<objective>
Expose schedule control and practice selection in the UI.

Purpose: allow users to follow the schedule, switch tracks, and choose practices outside the schedule.
Output: settings and practice screens updated to reflect program mode and recommendations.
</objective>

<execution_context>
@/Users/richardyu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardyu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-practice-progression--schedule-control/02-RESEARCH.md

@app/(tabs)/practice.tsx
@app/(tabs)/settings.tsx
@app/(tabs)/index.tsx
@lib/practices.ts
@lib/storage.ts
</context>

<tasks>

<task type="auto">
  <name>Add track selection + continuation controls</name>
  <files>app/(tabs)/settings.tsx, lib/storage.ts</files>
  <action>
    - Add UI controls to select program mode (standard 6-week / extended 8-week / open training).
    - If open training is selected, allow choosing 1–2 focus practices for rotation.
    - Persist selections in settings via storage helpers.
  </action>
  <verify>
    - Settings UI updates program mode and persists after reload.
  </verify>
  <done>
    - Users can choose a track and (optionally) a custom practice set.
  </done>
</task>

<task type="auto">
  <name>Show recommended practice + manual selection</name>
  <files>app/(tabs)/practice.tsx, app/(tabs)/index.tsx, lib/practices.ts</files>
  <action>
    - Add a "Recommended Today" card that uses program-aware schedule logic.
    - Keep the full practice list for manual selection outside the schedule.
    - Ensure home screen shows the recommended practice name for the active track.
  </action>
  <verify>
    - Recommended practice updates when track or custom set changes.
  </verify>
  <done>
    - Users can follow the schedule or choose any practice.
  </done>
</task>

</tasks>

<verification>
- Track selection affects recommended practice.
- Manual practice selection remains available.
</verification>

<success_criteria>
- Users can opt into extended training and still select practices freely.
</success_criteria>

<output>
After completion, create `.planning/phases/02-practice-progression--schedule-control/02-02-SUMMARY.md`
</output>
