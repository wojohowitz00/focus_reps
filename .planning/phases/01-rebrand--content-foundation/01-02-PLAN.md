---
phase: 01-rebrand--content-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - types/index.ts
  - lib/practices.ts
  - lib/practiceInstructions.ts
  - lib/storage.ts
  - __tests__/lib/practices.test.ts
  - __tests__/lib/storage.test.ts
autonomous: true
must_haves:
  truths:
    - "User sees four renamed Focus Reps practices with original descriptions"
    - "Practice scripts are original and contain no Peak Mind language"
    - "Existing sessions with old practice IDs still render safely"
  artifacts:
    - path: "types/index.ts"
      provides: "Updated PracticeType union"
    - path: "lib/practices.ts"
      provides: "New practice definitions + schedule"
    - path: "lib/practiceInstructions.ts"
      provides: "Original practice scripts"
  key_links:
    - from: "lib/practices.ts"
      to: "types/index.ts"
      via: "PracticeType"
      pattern: "PracticeType"
    - from: "lib/practiceInstructions.ts"
      to: "lib/practices.ts"
      via: "matching practice IDs"
      pattern: "id:"
---

<objective>
Rename the four core practices and replace all scripts with original Focus Reps content.

Purpose: Remove Peak Mind IP and establish Focus Reps practice identity.
Output: Updated practice IDs, descriptions, and scripts + compatibility for old IDs.
</objective>

<execution_context>
@/Users/richardyu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardyu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Content + schedule files
@types/index.ts
@lib/practices.ts
@lib/practiceInstructions.ts
@lib/storage.ts
@__tests__/lib/practices.test.ts
@__tests__/lib/storage.test.ts
</context>

<tasks>

<task type="auto">
  <name>Define new practice IDs + update schedule + types</name>
  <files>types/index.ts, lib/practices.ts, __tests__/lib/practices.test.ts</files>
  <action>
    - Choose 4 original practice IDs + names (kebab‑case IDs) aligned to Focus Reps.
    - Update `PracticeType` union with new IDs.
    - Update `practiceDefinitions` to use new names + descriptions.
    - Update schedule logic in `getPracticeForWeekAndDay` to use new IDs.
    - Update tests in `__tests__/lib/practices.test.ts` to reflect new IDs.
  </action>
  <verify>
    - `rg -n "find-your-flashlight|body-scan|river-of-thought|connection-practice" lib types __tests__` returns no matches.
  </verify>
  <done>
    - Practice IDs, names, and schedule use the new Focus Reps practice set.
  </done>
</task>

<task type="auto">
  <name>Rewrite practice scripts with original Focus Reps content</name>
  <files>lib/practiceInstructions.ts</files>
  <action>
    - Replace all scripts with original language (no Peak Mind phrasing).
    - Keep the structure fields intact (setup, steps, pitfalls, keysToSuccess).
    - Ensure scripts are concise and aligned to deliberate‑practice framing (reps, feedback, focus).
  </action>
  <verify>
    - `rg -n "Peak Mind|flashlight|river of thought|connection practice" lib/practiceInstructions.ts` returns no matches.
  </verify>
  <done>
    - New scripts exist for each practice with original text.
  </done>
</task>

<task type="auto">
  <name>Handle legacy practice IDs in storage</name>
  <files>lib/storage.ts, __tests__/lib/storage.test.ts</files>
  <action>
    - Add a small normalization step when reading sessions: map old practice IDs to new ones.
    - Keep mapping isolated (e.g., helper in storage) and cover with a unit test.
  </action>
  <verify>
    - `npm test -- __tests__/lib/storage.test.ts` passes.
  </verify>
  <done>
    - Existing stored sessions with old IDs render safely under new practice names.
  </done>
</task>

</tasks>

<verification>
- Old practice IDs removed from codebase.
- Practice scripts are original and aligned to Focus Reps.
- Storage gracefully handles legacy IDs.
</verification>

<success_criteria>
- All practices are renamed and backed by original scripts without Peak Mind language.
</success_criteria>

<output>
After completion, create `.planning/phases/01-rebrand--content-foundation/01-02-SUMMARY.md`
</output>
